(()=>{"use strict";const root=document.getElementById("mmdPathV27");if(!root||root.dataset.ready==="true")return;root.dataset.ready="true";const incoming=new URLSearchParams(location.search),keep=["t","code","promo","liff.state","from","payment_ref","session_id"];root.querySelectorAll("[data-preserve-link]").forEach(link=>{try{const url=new URL(link.getAttribute("href"),location.origin);keep.forEach(key=>{const value=incoming.get(key);if(value&&!url.searchParams.has(key))url.searchParams.set(key,value)});link.setAttribute("href",url.pathname+url.search+url.hash)}catch(_){}});const items=[...root.querySelectorAll("[data-reveal]")];if(!("IntersectionObserver" in window)||matchMedia("(prefers-reduced-motion: reduce)").matches){items.forEach(item=>item.classList.add("is-visible"))}else{root.classList.add("is-enhanced");const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{rootMargin:"0px 0px -7%",threshold:.08});items.forEach(item=>observer.observe(item))}const year=root.querySelector("[data-year]");if(year)year.textContent=String(new Date().getFullYear())})();
