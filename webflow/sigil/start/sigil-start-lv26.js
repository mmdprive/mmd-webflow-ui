(()=>{"use strict";const root=document.getElementById("sigilStartV26");if(!root||root.dataset.ready==="true")return;root.dataset.ready="true";const params=new URLSearchParams(location.search),preserve=["t","code","promo","liff.state","from","payment_ref","session_id"];function build(raw){try{const url=new URL(raw,location.origin);preserve.forEach(key=>{const value=params.get(key);if(value&&!url.searchParams.has(key))url.searchParams.set(key,value)});return url.origin===location.origin?url.pathname+url.search+url.hash:url.href}catch(_){return raw}}root.querySelectorAll("[data-preserve-link]").forEach(link=>{const href=link.getAttribute("href");if(href&&href.charAt(0)!=="#")link.setAttribute("href",build(href))});const items=[...root.querySelectorAll("[data-reveal]")];if(!("IntersectionObserver" in window)||matchMedia("(prefers-reduced-motion: reduce)").matches){items.forEach(item=>item.classList.add("is-visible"))}else{root.classList.add("is-enhanced");const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{rootMargin:"0px 0px -7%",threshold:.08});items.forEach(item=>observer.observe(item))}const year=root.querySelector("[data-year]");if(year)year.textContent=String(new Date().getFullYear())})();
